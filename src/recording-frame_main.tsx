import React from "react";
import ReactDOM from "react-dom/client";
import RecordingFrame from "./pages/RecordingFrame";

ReactDOM.createRoot(document.getElementById("frame-root")!).render(
  <React.StrictMode>
    <RecordingFrame />
  </React.StrictMode>,
);
